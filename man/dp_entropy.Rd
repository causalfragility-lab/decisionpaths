% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_entropy.R
\name{dp_entropy}
\alias{dp_entropy}
\title{Compute Decision Path Entropy}
\usage{
dp_entropy(x, by = NULL, mutual_info = FALSE)
}
\arguments{
\item{x}{A \code{decision_path} object from \code{\link{dp_build}}.}

\item{by}{Optional character string naming a group variable for stratified
entropy. Defaults to \code{x$group_var}.}

\item{mutual_info}{Logical. Compute mutual information between path and
group? Default \code{FALSE}.}
}
\value{
An object of class \code{dp_entropy}, a named list with:
\describe{
\item{entropy}{Shannon entropy H in bits.}
\item{normalized_entropy}{H divided by log2(number of unique paths).}
\item{path_frequencies}{Tibble of path strings, counts, and proportions.}
\item{n_unique_paths}{Number of unique decision paths observed.}
\item{by_group}{By-group entropy tibble (NULL if no group variable).}
\item{mutual_info}{Mutual information in bits (NULL if not requested).}
\item{group_var}{Group variable name used.}
}
}
\description{
Computes Shannon entropy (H) of the decision-path distribution, grounded
in information theory (Shannon, 1948). Entropy is measured in bits.
}
\examples{
dat <- data.frame(
  id       = c(1, 1, 2, 2),
  time     = c(1, 2, 1, 2),
  decision = c(0, 1, 1, 0)
)
dp  <- dp_build(dat, id, time, decision)
ent <- dp_entropy(dp)
print(ent)

}
\references{
Shannon, C. E. (1948). A mathematical theory of communication.
\emph{Bell System Technical Journal}, 27(3), 379--423.
}
